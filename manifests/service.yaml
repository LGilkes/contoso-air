apiVersion: v1
kind: Service
metadata:
  name: contoso-air2
  namespace: dev
  labels:
    app.kubernetes.io/name: contoso-air2
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: contoso-air2
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000